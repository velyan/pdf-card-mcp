[project]
name = "pdf-card-mcp"
version = "0.1.0"
description = "Local-first MCP tool that converts PDFs into standalone card-based HTML readers."
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
authors = [{ name = "PDF Card MCP Contributors" }]
keywords = ["mcp", "pdf", "html", "accessibility", "documents"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Text Processing :: Markup :: HTML",
]
dependencies = [
  "fastmcp>=3,<4",
  "mcp[cli]>=1.10.0",
  "pdfplumber>=0.11.0",
  "pillow>=10.4.0",
  "pymupdf>=1.24.0",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.2.0",
  "pytest-cov>=5.0.0",
]
docling = [
  "docling>=2.0.0",
]
ocr = [
  "pytesseract>=0.3.13",
]

[project.scripts]
pdf-card-mcp = "pdf_card_mcp.cli:main"
pdf-card-mcp-server = "pdf_card_mcp.server:main"

[build-system]
requires = ["hatchling>=1.25.0"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/pdf_card_mcp"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py311"
